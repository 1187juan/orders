import styled from '@emotion/styled'

export const Nav = styled('nav')({
	width: '100%',
	height: '100%',
	maxWidth: '1024px',
	display: 'flex',
	justifyContent: 'space-between',
	alignItems: 'center',
})
